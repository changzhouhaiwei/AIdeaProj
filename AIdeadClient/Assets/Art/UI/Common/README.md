# Common UI Parts

跨界面复用的通用零件。`layout.json` 里用：

```json
"sprite": "Common/btn_close.png"
```

Builder 会从 `Assets/Art/UI/Common/Parts/` 加载。

当前已收录（木纹风格）：

- `btn_close.png` — 红底关闭
- `title_banner.png` — 标题牌
- `icon_info.png` — 信息图标（备用）
